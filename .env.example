# ---------------------------------------------
# API
# ---------------------------------------------
# Базовый URL backend API (используется в lib/axios.ts).
# Запросы идут с withCredentials: true, поэтому backend должен
# разрешать CORS с credentials для домена фронтенда.
NEXT_PUBLIC_API_URL=http://localhost:3001
